{
  "watch": ["src", "tsconfig.json", ".env"],
  "ext": "ts",
  "ignore": ["src/**/*.test.ts"],
  "exec": "npm run build && PORT=7777 npm start",
  "env": {
    "NODE_DEBUG": 9229,
    "NODE_ENV": "development"
  }
}